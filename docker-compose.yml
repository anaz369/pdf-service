services:
  pdf-service:
    build: .
    container_name: pdf-service
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - puppeteer-service

  puppeteer-service:
    build: ./puppeteer-service
    container_name: puppeteer-service
    restart: always
    ports:
      - "3001:3001"
